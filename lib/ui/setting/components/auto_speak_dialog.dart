import 'package:flutter/material.dart';

class AutoSpeakDialog extends StatelessWidget {
  const AutoSpeakDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
